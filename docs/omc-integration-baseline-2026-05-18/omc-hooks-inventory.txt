OMC Post-Install Hooks Inventory — 2026-05-18
==============================================

Captured immediately after `/plugin install oh-my-claudecode` on
2026-05-18 to document what hooks OMC registered, against which the
surgery in Task 3 will be applied.

hooks.json path: C:\Users\riley\.claude\plugins\cache\omc\oh-my-claudecode\4.14.0\hooks\hooks.json
OMC version installed: 4.14.0 (gitCommitSha 90f19265cabb8f35a11e932aa89f206380de0554, installedAt 2026-05-18T19:58:28.220Z)
Plugin source: oh-my-claudecode@omc (user scope)

## Hooks present per event

UserPromptSubmit (2 hooks, matcher "*"):
  - scripts/keyword-detector.mjs   (timeout 5s)
  - scripts/skill-injector.mjs     (timeout 3s)

SessionStart (5 hooks across 3 matchers):
  matcher "*" (3 hooks):
    - scripts/session-start.mjs           (timeout 5s)
    - scripts/project-memory-session.mjs  (timeout 5s)
    - scripts/wiki-session-start.mjs      (timeout 5s)
  matcher "init" (1 hook):
    - scripts/setup-init.mjs              (timeout 30s)
  matcher "maintenance" (1 hook):
    - scripts/setup-maintenance.mjs       (timeout 60s)

PreToolUse (1 hook, matcher "*"):
  - scripts/pre-tool-enforcer.mjs   (timeout 3s)

PermissionRequest (1 hook, matcher "Bash"):
  - scripts/permission-handler.mjs  (timeout 5s)

PostToolUse (3 hooks, matcher "*"):
  - scripts/post-tool-verifier.mjs          (timeout 3s)
  - scripts/project-memory-posttool.mjs     (timeout 3s)
  - scripts/post-tool-rules-injector.mjs    (timeout 3s)

PostToolUseFailure (1 hook, matcher "*"):
  - scripts/post-tool-use-failure.mjs  (timeout 3s)

SubagentStart (1 hook, matcher "*"):
  - scripts/subagent-tracker.mjs start  (timeout 3s)

SubagentStop (2 hooks, matcher "*"):
  - scripts/subagent-tracker.mjs stop   (timeout 5s)
  - scripts/verify-deliverables.mjs     (timeout 5s)

PreCompact (3 hooks, matcher "*"):
  - scripts/pre-compact.mjs              (timeout 10s)
  - scripts/project-memory-precompact.mjs (timeout 5s)
  - scripts/wiki-pre-compact.mjs         (timeout 3s)

Stop (3 hooks, matcher "*"):
  - scripts/context-guard-stop.mjs  (timeout 5s)
  - scripts/persistent-mode.mjs     (timeout 10s)
  - scripts/code-simplifier.mjs     (timeout 5s)

SessionEnd (2 hooks, matcher "*"):
  - scripts/session-end.mjs       (timeout 30s)
  - scripts/wiki-session-end.mjs  (timeout 30s)

All hook commands shell out to: node "$CLAUDE_PLUGIN_ROOT"/scripts/run.cjs <hook-script>

## Surgery targets (cross-reference with Task 3)

- UserPromptSubmit:keyword-detector.mjs  — PRESENT — DISABLE (magic-keyword auto-escalation)
- UserPromptSubmit:skill-injector.mjs    — PRESENT — DISABLE (magic-keyword auto-escalation)
- Stop:persistent-mode.mjs               — PRESENT — SCOPE TO EXPLICIT MODES ONLY
- SessionEnd:wiki-session-end.mjs        — PRESENT — DISABLE (don't auto-write decisions to wiki)

All four surgery targets confirmed present in the installed hooks.json.

## Deltas from research baseline

Hooks present that weren't on the expected list:
  - None at the event/hook level. All researched hook names are present.
  - Additional SessionStart matchers ("init", "maintenance") were noted as
    "possibly present" in research and are confirmed here. They are NOT
    surgery targets — they only fire when a session explicitly carries
    the "init" or "maintenance" matcher, not on normal session starts.

Expected hooks not found:
  - None. Every hook listed in the research expectation set is present in
    the installed hooks.json.

## Install errors (if any)

No errors located programmatically.

Checked:
  - ~/.claude/logs/                       — directory does not exist
  - ~/.claude/plugins/                    — no errors.json or *.log files
  - ~/.claude/plugins/data/               — empty (only sibling superpowers
                                            and vercel data dirs; no omc entry)
  - ~/.claude/plugins/cache/omc/oh-my-claudecode/4.14.0/
                                          — no error markers; .in_use sentinel
                                            present (indicates active plugin)
  - ~/.claude/telemetry/                  — only pre-install (2026-05-15)
                                            failed-event files; none post-install
                                            mention "omc" or plugin install errors
  - ~/.claude/debug/                      — empty

Observation: OMC's plugin.json declares "skills", "mcpServers", and "commands"
keys but does NOT declare a "hooks" key. Claude Code appears to auto-detect
./hooks/hooks.json regardless (the hooks are clearly active per the cache
layout and the post-install behavior Riley has been seeing). This MAY be the
source of the "Errors (1)" tab in the /plugin UI — a non-fatal manifest
warning — but it does not block the hook surgery plan, since the hooks are
loaded and Task 3 disables them by name via settings.local.json regardless
of how the manifest declares them.

If the error proves load-bearing later, Riley can paste the exact UI text
and we'll revisit. Surgery in Task 3 is unaffected.

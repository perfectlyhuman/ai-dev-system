# Handoffs archive

`/finish` archives the prior `HANDOFF.md` here (named by its `last_updated` date) before writing a fresh one. Preserves the session timeline — "what was the state in <month>?" is grep-able. Machine-managed by `/finish`; don't hand-edit.

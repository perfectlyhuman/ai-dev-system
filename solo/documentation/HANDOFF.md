---
last_updated: {{DATE}}
session_summary: "Project scaffolded via the setup command. No work sessions yet."
---

# Handoff — project bootstrap → first session

Read this before `/start`. This is the seed handoff written at setup; `/finish` replaces it after the first real session.

## What just happened (this session)

Project scaffolded from the Makerkit template with the ai-dev-system v2 bundle installed.

## Where to start the next session

1. **Read this file.** Then `/start` will surface the rest.
2. Run `/kickoff` for product discovery, or `/vision` if the direction is known.
3. The cloud agent stays dormant until the ROADMAP has `Owner: agent`, `Gate: —` tasks and a green gate.

## Known leftovers

- Engine registration PR (opened at setup) may still await your merge.

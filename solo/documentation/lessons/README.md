# Lessons — known traps / negative knowledge (single source of truth)

One file per non-obvious trap that cost time and would re-bite a future agent (human or Gilfoyle). This is the ONE place lessons live — chapters link here, they don't duplicate.

File: `YYYY-MM-DD-<short-slug>.md`. Frontmatter: `severity: low|medium|high`, `area: <area>`. Body: **What happened / What we learned / How to apply.**

Both executors use this dir: `/update-docs` and `/reflect` write here; the cloud agent scans it before touching a related area and writes a new lesson when it hits a trap mid-task.

## Index

| Date | Severity | Area | Lesson |
|---|---|---|---|
| (none yet) | | | |
